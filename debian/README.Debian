This package provides the SIP implementation pjsip (http://www.pjsip.org).
It includes the library, python-bindings (old and swig), java-bindings (swig) and samples.

The library is build with video-support for Ubuntu 14.04.
